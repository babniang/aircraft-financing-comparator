# Local dev: the frontend origin that may call this API
ALLOWED_ORIGIN=http://localhost:3000
# Optional regex to also allow Vercel preview deployments, e.g.:
# ALLOWED_ORIGIN_REGEX=https://.*-yourteam\.vercel\.app
